\documentclass{ximera}
\author{Jeffrey Kuan}
\title{Math 1050 HW 5.2 --- Graphs of Straight Lines}
\license{CC: 0}

\begin{document}

\begin{abstract}
    Section 5.2 --- Graphs of Straight Lines.
    Assigned exercises: 3, 8, 9, 13–29 odd, 35–51 odd, 63, 64, 65–109 every other odd.
\end{abstract}

\maketitle

SECTION 5.2 3, 8, 9, 13 – 29 odd, 35 – 51 odd, 63, 64, 65 – 109 every other odd. 3 \begin{enumerate} \item \begin{multipleChoice} \choice{It is a straight line.} \choice[correct]{It is not a straight line.} \end{multipleChoice} \item \begin{multipleChoice} \choice[correct]{It is a straight line.} \choice{It is not a straight line.} \end{multipleChoice} \item \begin{multipleChoice} \choice{It is a straight line.} \choice[correct]{It is not a straight line.} \end{multipleChoice} \item \begin{multipleChoice} \choice[correct]{It is a straight line.} \choice{It is not a straight line.} \end{multipleChoice} \item \begin{multipleChoice} \choice{It is a straight line.} \choice[correct]{It is not a straight line.} \end{multipleChoice} \end{enumerate}

\begin{problem}
\textbf{3.} State whether the graph of the equation is a straight line.
  \\[2pt]
  \textbf{a.}~$y = x^2 + 1$ \quad \textbf{b.}~$y = -x$ \quad
  \textbf{c.}~$y = \dfrac{1}{x}$ \quad \textbf{d.}~$y = 2 - \dfrac{1}{2}x$ \quad
  \textbf{e.}~$y = \sqrt{x} - 1$

\begin{multipleChoice}
\choice[correct]{b, d}
\choice{none of them}
\choice{a, c, e}
\choice{all of them}
\end{multipleChoice}
\end{problem}

\begin{problem}
\textbf{8.} Describe the graph of a line that has an $x$-intercept but no $y$-intercept.

\begin{multipleChoice}
\choice{A horizontal line}
\choice{A slanted line}
\choice{A line through the origin}
\choice[correct]{A vertical line}
\end{multipleChoice}

\end{problem}

\begin{problem}
\textbf{9.} Describe the graph of a line that has a $y$-intercept but no $x$-intercept.

\begin{multipleChoice}
\choice{A vertical line}
\choice{A slanted line}
\choice{A line through the origin}
\choice[correct]{A horizontal line}
\end{multipleChoice}

\end{problem}

\begin{problem}
\textbf{13.} Is $(3, 4)$ a solution of $y = -x + 7$?

\begin{multipleChoice}
\choice{No, it is not a solution.}
\choice[correct]{Yes, it is a solution.}
\end{multipleChoice}
\end{problem}

\begin{problem}
\textbf{15.} Is $(-1, 2)$ a solution of $y = \frac{1}{2}x - 1$?

\begin{multipleChoice}
\choice[correct]{No, it is not a solution.}
\choice{Yes, it is a solution.}
\end{multipleChoice}
\end{problem}

\begin{problem}
\textbf{17.} Is $(4, 1)$ a solution of $y = \frac{1}{4}x + 1$?

\begin{multipleChoice}
\choice[correct]{No, it is not a solution.}
\choice{Yes, it is a solution.}
\end{multipleChoice}
\end{problem}

\begin{problem}
\textbf{19.} Is $(0, 4)$ a solution of $y = \frac{3}{4}x + 4$?

\begin{multipleChoice}
\choice{No, it is not a solution.}
\choice[correct]{Yes, it is a solution.}
\end{multipleChoice}
\end{problem}

\begin{problem}
\textbf{21.} Is $(0, 0)$ a solution of $y = 3x + 2$?

\begin{multipleChoice}
\choice[correct]{No, it is not a solution.}
\choice{Yes, it is a solution.}
\end{multipleChoice}
\end{problem}

\begin{problem}
\textbf{23.} Find the ordered-pair solution of $y = 3x - 2$ that corresponds to $x = 3$.

\textbf{Answer:} $\answer{(3,7)}$
\end{problem}

\begin{problem}
\textbf{25.} Find the ordered-pair solution of $y = \frac{2}{3}x - 1$ that corresponds to
  $x = 6$.

The answer is $\answer{(6,3)}$ (enter your answer as an ordered pair with no spaces).
\end{problem}

\begin{problem}
\textbf{27.} Find the ordered-pair solution of $y = -3x + 1$ that corresponds to $x = 0$.

The answer is $\answer{(0,1)}$ (enter your answer as an ordered pair with no spaces).
\end{problem}

\begin{problem}
\textbf{29.} Find the ordered-pair solution of $y = \frac{2}{5}x + 2$ that corresponds to
  $x = -5$.

The answer is $\answer{(-5,0)}$ (enter your answer as an ordered pair with no spaces).
\end{problem}

\begin{problem}
\textbf{35.} \emph{Graph.}\quad $y = 2x - 3$        \figbox{\gridsm}

\begin{hint}
\textbf{Solution.} Solved for $y$ the equation is $y=2x - 3$, so the slope is $2$ and the $y$-intercept is $(0,-3)$. Two more lattice points on the line are $(-1,-5)$ and $(1,-1)$.
\end{hint}
\end{problem}

\begin{problem}
\textbf{37.} \emph{Graph.}\quad $y = \frac{1}{3}x$  \figbox{\gridsm}

\begin{hint}
\textbf{Solution.} Solved for $y$ the equation is $y=\frac{x}{3}$, so the slope is $\frac{1}{3}$ and the $y$-intercept is $(0,0)$. Two more lattice points on the line are $(-3,-1)$ and $(3,1)$.
\end{hint}
\end{problem}

\begin{problem}
\textbf{39.} \emph{Graph.}\quad $y = \frac{2}{3}x - 1$ \figbox{\gridsm}

\begin{hint}
\textbf{Solution.} Solved for $y$ the equation is $y=\frac{2x}{3} - 1$, so the slope is $\frac{2}{3}$ and the $y$-intercept is $(0,-1)$. Two more lattice points on the line are $(-3,-3)$ and $(3,1)$.
\end{hint}
\end{problem}

\begin{problem}
\textbf{41.} \emph{Graph.}\quad $y = -\frac{1}{4}x + 2$ \figbox{\gridsm}

\begin{hint}
\textbf{Solution.} Solved for $y$ the equation is $y=2 -\frac{x}{4}$, so the slope is $-\frac{1}{4}$ and the $y$-intercept is $(0,2)$. Two more lattice points on the line are $(-4,3)$ and $(4,1)$.
\end{hint}
\end{problem}

\begin{problem}
\textbf{43.} \emph{Graph.}\quad $y = -\frac{2}{5}x + 1$ \figbox{\gridsm}

\begin{hint}
\textbf{Solution.} Solved for $y$ the equation is $y=1 -\frac{2x}{5}$, so the slope is $-\frac{2}{5}$ and the $y$-intercept is $(0,1)$. Two more lattice points on the line are $(-5,3)$ and $(5,-1)$.
\end{hint}
\end{problem}

\begin{problem}
\textbf{45.} \emph{Graph.}\quad $y = 2x - 4$        \figbox{\gridsm}

\begin{hint}
\textbf{Solution.} Solved for $y$ the equation is $y=2x - 4$, so the slope is $2$ and the $y$-intercept is $(0,-4)$. Two more lattice points on the line are $(1,-2)$ and $(2,0)$.
\end{hint}
\end{problem}

\begin{problem}
\textbf{47.} \emph{Graph.}\quad $y = -x + 2$        \figbox{\gridsm}

\begin{hint}
\textbf{Solution.} Solved for $y$ the equation is $y=2 - x$, so the slope is $-1$ and the $y$-intercept is $(0,2)$. Two more lattice points on the line are $(-1,3)$ and $(1,1)$.
\end{hint}
\end{problem}

\begin{problem}
\textbf{49.} \emph{Graph.}\quad $y = -\frac{2}{3}x + 1$ \figbox{\gridsm}

\begin{hint}
\textbf{Solution.} Solved for $y$ the equation is $y=1 -\frac{2x}{3}$, so the slope is $-\frac{2}{3}$ and the $y$-intercept is $(0,1)$. Two more lattice points on the line are $(-3,3)$ and $(3,-1)$.
\end{hint}
\end{problem}

\begin{problem}
\textbf{51.} \emph{Graph.}\quad $y = -3x + 2$       \figbox{\gridsm}

\begin{hint}
\textbf{Solution.} Solved for $y$ the equation is $y=2 - 3x$, so the slope is $-3$ and the $y$-intercept is $(0,2)$. Two more lattice points on the line are $(-1,5)$ and $(1,-1)$.
\end{hint}
\end{problem}

\begin{problem}
\textbf{63.} The graph of the equation $y = 7$ is a \blank{} line passing through the point
  whose coordinates are $(0, 7)$.

\begin{multipleChoice}
\choice{A vertical line}
\choice{A slanted line}
\choice{A line through the origin}
\choice[correct]{A horizontal line}
\end{multipleChoice}

\end{problem}

\begin{problem}
\textbf{64.} The graph of the equation $x = -10$ is a \blank{} line passing through the point
  whose coordinates are $(-10, 0)$.

\begin{multipleChoice}
\choice{A horizontal line}
\choice{A slanted line}
\choice{A line through the origin}
\choice[correct]{A vertical line}
\end{multipleChoice}

\end{problem}

\begin{problem}
\textbf{65.} \emph{Write the equation in the form $y = mx + b$.}\quad $3x + y = 10$

$y = \answer{-3}x + \answer{10}$.
\end{problem}

\begin{problem}
\textbf{69.} \emph{Write the equation in the form $y = mx + b$.}\quad $3x + 2y = 6$

$y = \answer{-3/2}x + \answer{3}$.
\end{problem}

\begin{problem}
\textbf{73.} \emph{Write the equation in the form $y = mx + b$.}\quad $2x + 7y = 14$

$y = \answer{-2/7}x + \answer{2}$.
\end{problem}

\begin{problem}
\textbf{77.} \emph{Graph.}\quad $3x + y = 3$   \figbox{\gridsm}

\begin{hint}
\textbf{Solution.} Solved for $y$ the equation is $y=3 - 3x$, so the slope is $-3$ and the $y$-intercept is $(0,3)$. Two more lattice points on the line are $(1,0)$ and $(2,-3)$.
\end{hint}
\end{problem}

\begin{problem}
\textbf{81.} \emph{Graph.}\quad $x - 2y = 4$   \figbox{\gridsm}

\begin{hint}
\textbf{Solution.} Solved for $y$ the equation is $y=\frac{x}{2} - 2$, so the slope is $\frac{1}{2}$ and the $y$-intercept is $(0,-2)$. Two more lattice points on the line are $(-2,-3)$ and $(2,-1)$.
\end{hint}
\end{problem}

\begin{problem}
\textbf{85.} \emph{Graph.}\quad $y = 4$        \figbox{\gridsm}

\begin{hint}
\textbf{Solution.} This is a horizontal line through $(0,4)$: every point on it has $y=4$.
\end{hint}
\end{problem}

\begin{problem}
\textbf{89.} \emph{Find the $x$- and $y$-intercepts.}\quad $x - y = 3$

The \(x\)--intercept is $\answer{(3,0)}$ and the \(y\)--intercept is $\answer{(0,-3)}$. 
Enter your answers as ordered pairs without spaces.
\end{problem}

\begin{problem}
\textbf{93.} \emph{Find the $x$- and $y$-intercepts.}\quad $x - 5y = 10$

The \(x\)--intercept is $\answer{(10,0)}$ and the \(y\)--intercept is $\answer{(0,-2)}$. 
Enter your answers as ordered pairs without spaces.
\end{problem}

\begin{problem}
\textbf{97.} \emph{Find the $x$- and $y$-intercepts.}\quad $2x - 3y = 0$

The \(x\)--intercept is $\answer{(0,0)}$ and the \(y\)--intercept is $\answer{(0,0)}$. 
Enter your answers as ordered pairs without spaces.
\end{problem}

\begin{problem}
\textbf{101.} \emph{Graph by using $x$- and $y$-intercepts.}\quad $5x + 2y = 10$ \figbox{\gridsm}

\begin{hint}
\textbf{Solution.} Solved for $y$ the equation is $y=5 -\frac{5x}{2}$, so the slope is $-\frac{5}{2}$ and the $y$-intercept is $(0,5)$. Two more lattice points on the line are $(2,0)$ and $(4,-5)$.
\end{hint}
\end{problem}

\begin{problem}
\textbf{105.} \emph{Graph by using $x$- and $y$-intercepts.}\quad $2x + 3y = 6$  \figbox{\gridsm}

\begin{hint}
\textbf{Solution.} Solved for $y$ the equation is $y=2 -\frac{2x}{3}$, so the slope is $-\frac{2}{3}$ and the $y$-intercept is $(0,2)$. Two more lattice points on the line are $(-3,4)$ and $(3,0)$.
\end{hint}
\end{problem}

\begin{problem}
\textbf{109.} \emph{Graph by using $x$- and $y$-intercepts.}\quad $x - 3y = 6$   \figbox{\gridsm}

\begin{hint}
\textbf{Solution.} Solved for $y$ the equation is $y=\frac{x}{3} - 2$, so the slope is $\frac{1}{3}$ and the $y$-intercept is $(0,-2)$. Two more lattice points on the line are $(-3,-3)$ and $(3,-1)$.
\end{hint}
\end{problem}

\end{document}
